name: CI — Bandit SAST

on:
  push:
    branches: ['**']
  pull_request:
    branches: ['**']

jobs:
  bandit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v4
        with:
          python-version: '3.x'
      - run: |
          pip install bandit
          bandit -r .
